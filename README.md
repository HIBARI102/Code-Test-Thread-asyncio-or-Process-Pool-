# 🧪 Python Concurrency Lab: Asyncio, Threading, and Process Pool

โปรเจกต์นี้เป็นแล็บทดลองขนาดเล็กเพื่อสาธิตความแตกต่างระหว่างการทำงานแบบปกติ (Sequential) เปรียบเทียบกับการใช้เทคนิค Concurrency 3 รูปแบบหลักใน Python

---

## 📋 สรุปแนวคิดการเลือกใช้งาน

| เทคนิค | เหมาะสำหรับงานประเภท | กลไกการทำงาน | จุดเด่น |
| :--- | :--- | :--- | :--- |
| **Normal (Sync)** | งานทั่วไปที่ทำต่อเนื่องกัน | ทำทีละบรรทัด (Blocking) | เขียนง่าย ไม่ซับซ้อน |
| **Asyncio** | **I/O-bound** (เน้นการรอ) | Single-threaded, Event Loop | ประหยัดทรัพยากรที่สุดเมื่อต้องรอ Network |
| **Threading** | **I/O-bound** (งานพื้นหลัง) | Multi-threaded, OS Switching | ใช้ได้กับ Library ที่ไม่รองรับ Async |
| **Process Pool** | **CPU-bound** (คำนวณหนัก) | Multi-processing, Parallelism | ใช้ CPU ได้ครบทุก Core (หนีจาก GIL) |

---

## 🐢 0. Sequential: การทำงานแบบปกติ (Baseline)
**ไฟล์:** `00_sequential_comparison.py`

ก่อนจะดูความเร็ว เราต้องมีตัวเปรียบเทียบ (Baseline) เพื่อให้เห็นความต่าง:
* **กลไก:** โปรแกรมจะทำงานแบบ "ต่อคิว" (First-come, First-served) ถ้างานแรกไม่เสร็จ งานที่สองจะไม่ได้เริ่ม
* **ตัวอย่าง:** - ดึงสภาพอากาศ 3 เมือง เมืองละ 2 วินาที ใช้เวลารวม **6 วินาที**
    - เขียน Log 5 ไฟล์ ไฟล์ละ 1 วินาที ใช้เวลารวม **5 วินาที**
* **ปัญหา:** CPU และ Network จะว่างงานเป็นช่วงๆ เพราะต้องรอให้งานก่อนหน้าเสร็จสมบูรณ์ก่อน



---

## 🌩️ 1. Asyncio: ตรวจสอบสภาพอากาศ (Cooperative)
**ไฟล์:** `01_async_io_weather.py`

* **โจทย์:** จำลองการดึงข้อมูลอากาศจากหลายเมือง (I/O-bound)
* **กลไก:** ใช้ **Event Loop** และคีย์เวิร์ด `await` เพื่อบอกโปรแกรมว่า "ระหว่างที่รอการตอบกลับจาก Network ให้สลับไปส่ง Request ให้เมืองอื่นต่อทันที" 
* **ข้อดี:** ทำงานได้รวดเร็วภายใต้ Thread เดียว ไม่ต้องสร้างพนักงาน (Thread) หลายคนให้เปลืองแรม
* **ผลลัพธ์:** เวลารวมจะเหลือเพียงค่าเฉลี่ยของการรอ 1 งาน (ประมาณ 2 วินาที)



---

## 🧵 2. Threading: ระบบบันทึก Log (Preemptive)
**ไฟล์:** `02_threaded_logs.py`

* **โจทย์:** เขียนไฟล์ Log หลายไฟล์พร้อมกัน (I/O-bound แบบ Blocking)
* **กลไก:** ใช้ `ThreadPoolExecutor` สร้าง "พนักงาน (Threads)" มาช่วยรุมทำงาน ระบบปฏิบัติการ (OS) จะคอยสลับงาน (Context Switching) ให้พนักงานคนอื่นทำงานต่อทันทีเมื่อคนปัจจุบันติดสถานะรอ (Sleep/Disk I/O)
* **ข้อดี:** เหมาะกับ Library หรือคำสั่งจัดการไฟล์ที่ไม่มีฟังก์ชัน `async`
* **ผลลัพธ์:** งานเขียน 5 ไฟล์จะเสร็จในเวลาใกล้เคียงกับงานเดียว (ประมาณ 1 วินาที)



---

## ⚙️ 3. Process Pool: คำนวณเลขยกกำลัง (Parallelism)
**ไฟล์:** `03_process_pool_math.py`

* **โจทย์:** คำนวณเลขชุดใหญ่ (CPU-bound)
* **กลไก:** การคำนวณใน Python จะติด **GIL (Global Interpreter Lock)** ทำให้ Threading ช่วยได้ไม่เต็มที่ เราจึงต้องใช้ `ProcessPoolExecutor` เพื่อแตกตัว (Spawn) ออกเป็น Process ใหม่แยกกันจริงๆ 
* **ข้อดี:** สามารถใช้พลังประมวลผลของ CPU ได้ครบทุก Core (True Parallelism) 
* **ผลลัพธ์:** ความเร็วเพิ่มขึ้นแบบทวีคูณตามจำนวน Core ของคอมพิวเตอร์ที่ใช้งาน



---

## 🚀 วิธีการทดสอบ
ลองรันไฟล์ Baseline ก่อนเพื่อดูเวลาที่ใช้แบบปกติ แล้วรันไฟล์อื่นๆ เพื่อเปรียบเทียบ:

1. **รันตัวเปรียบเทียบแบบปกติ:**
   ```bash
   python 00_sequential_comparison.py
2. **รันตัวทดสอบ Asyncio:**
   ```bash
   python 01_async_io_weather.py
3. **รันตัวทดสอบ Threading:**
   ```bash
   python 02_threaded_logs.py
4. **รันตัวทดสอบ Process Pool:**
   ```bash
   python 03_process_pool_math.py

