# Code-Test-Thread-asyncio-or-Process-Pool-

# 🧪 Python Concurrency Lab

โปรเจกต์ขนาดเล็กเพื่อทดสอบการทำงานของ Concurrency 3 รูปแบบใน Python

## 1. Asyncio (Single Thread, Cooperative Multitasking)
- **เหมาะสำหรับ:** I/O-bound ที่ต้องรอคอยนานๆ เช่น การเรียก API, การดึงข้อมูลจาก Database
- **โจทย์:** จำลองการเช็คสภาพอากาศจากหลายเมืองพร้อมกัน โดยไม่ต้องรอให้เมืองแรกเสร็จก่อน

## 2. Threading (Multi-threads, Preemptive Multitasking)
- **เหมาะสำหรับ:** I/O-bound ที่ Library ไม่รองรับ async หรือการทำงานเบื้องหลัง (Background Tasks)
- **โจทย์:** การเขียนบันทึกข้อมูล (Logging) ลงไฟล์หลายๆ ไฟล์พร้อมกันเสมือนมีระบบ Agent ทำงานแยกกัน

## 3. Process Pool (Multi-processing, Parallelism)
- **เหมาะสำหรับ:** CPU-bound หรืองานที่ใช้พลังประมวลผลสูง เพื่อหนีจากข้อจำกัดของ GIL (Global Interpreter Lock)
- **โจทย์:** การหาผลรวมของเลขยกกำลัง (Power Sum) ในช่วงตัวเลขขนาดใหญ่

# 🧪 Python Concurrency Lab: Asyncio, Threading, and Process Pool

โปรเจกต์ขนาดเล็กนี้สร้างขึ้นเพื่อสาธิตและเปรียบเทียบการทำงานของ Concurrency 3 รูปแบบหลักใน Python โดยใช้โจทย์ที่เหมาะสมกับคุณสมบัติของแต่ละเทคนิค

---

## 📋 สรุปแนวคิดการเลือกใช้งาน

| เทคนิค | เหมาะสำหรับงานประเภท | กลไกการทำงาน | จุดเด่น |
| :--- | :--- | :--- | :--- |
| **Asyncio** | **I/O-bound** (เน้นการรอ) | Single-threaded, Event Loop | ประหยัดทรัพยากรที่สุด เมื่อต้องรอ Network/API |
| **Threading** | **I/O-bound** (งานเบื้องหลัง) | Multi-threaded, OS Switching | ใช้ง่ายกับ Library เก่าที่ไม่ได้ออกแบบมาเป็น Async |
| **Process Pool** | **CPU-bound** (คำนวณหนัก) | Multi-processing, Parallelism | ใช้ CPU ได้ครบทุก Core (ทะลุข้อจำกัด GIL) |

---

## 🌩️ 1. Asyncio: ตรวจสอบสภาพอากาศ (Cooperative)
**ไฟล์:** `01_async_io_weather.py`

### **อธิบายโค้ด:**
ในโจทย์นี้เราจำลองการดึงข้อมูลสภาพอากาศจาก 5 เมือง ซึ่งต้องรอ Network I/O เมืองละ 2 วินาที
* **`async def`**: ประกาศว่าฟังก์ชันนี้เป็น Coroutine ที่สามารถหยุดพักงานได้
* **`await asyncio.sleep(2)`**: นี่คือจุดสำคัญ โปรแกรมจะบอกว่า "ฉันกำลังรอข้อมูลนะ" แล้วคืนอำนาจให้ **Event Loop** ไปเริ่มดึงข้อมูลเมืองถัดไปทันทีโดยไม่ต้องรอให้เมืองแรกเสร็จ
* **`asyncio.gather()`**: สั่งให้ภารกิจทั้งหมดเริ่มทำงานพร้อมกัน

**ผลลัพธ์:** แม้จะมี 5 เมือง แต่เวลารวมที่ใช้จะอยู่ที่ประมาณ **2 วินาทีเท่านั้น** (เท่ากับเวลาที่นานที่สุดของหนึ่งงาน)



---

## 🧵 2. Threading: ระบบบันทึก Log (Preemptive)
**ไฟล์:** `02_threaded_logs.py`

### **อธิบายโค้ด:**
จำลองการเขียนไฟล์ Log หลายไฟล์พร้อมกัน ซึ่งเป็นงาน I/O-bound
* **`ThreadPoolExecutor`**: สร้าง "พนักงาน (Threads)" ขึ้นมา 5 คนเพื่อแยกกันทำงาน
* **Context Switching**: ระบบปฏิบัติการ (OS) จะเป็นคนคอยสลับคิวให้พนักงานแต่ละคนอย่างรวดเร็ว หากพนักงานคนไหนติดงานเขียนไฟล์ (I/O) OS จะส่งงานให้พนักงานคนอื่นทำทันที
* **Memory Sharing**: ทุก Thread ทำงานอยู่ในโปรแกรมเดียวกัน (Process เดียวกัน) ทำให้ประหยัดแรมกว่าการสร้าง Process ใหม่

**ผลลัพธ์:** งานเขียนไฟล์ 5 ไฟล์เสร็จสิ้นพร้อมกันโดยใช้เวลารวมเพียงเล็กน้อย



---

## ⚙️ 3. Process Pool: คำนวณเลขยกกำลัง (Parallelism)
**ไฟล์:** `03_process_pool_math.py`

### **อธิบายโค้ด:**
โจทย์คำนวณเลขมหาศาลซึ่งกินพลัง CPU 100% งานประเภทนี้ Threading จะช่วยไม่ได้มากเพราะติด **GIL (Global Interpreter Lock)**
* **`ProcessPoolExecutor`**: โปรแกรมจะทำการ **"แตกตัว" (Spawn)** ออกเป็นโปรแกรมใหม่แยกจากกันตามจำนวน CPU Core ที่คุณมี
* **Independent Execution**: แต่ละ Process จะมีพื้นที่หน่วยความจำและ Python Interpreter ของตัวเอง ทำให้รันงานบน Core 1, 2, 3 ได้พร้อมกันจริงๆ (Parallel)
* **Breaking the GIL**: เป็นวิธีเดียวใน Python ที่จะทำให้ CPU รันเต็ม 100% ทุก Core ได้พร้อมกัน

**ผลลัพธ์:** การคำนวณเลข 4 ชุดใหญ่จะเสร็จไวขึ้นอย่างมากเมื่อเทียบกับการนั่งรอคำนวณทีละชุด



---

## 🚀 วิธีการทดสอบ
ลองรันแต่ละสคริปต์เพื่อดูการใช้เวลาและพฤติกรรมของโปรแกรม:
```bash
python 01_async_io_weather.py
python 02_threaded_logs.py
python 03_process_pool_math.py